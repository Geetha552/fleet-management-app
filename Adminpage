<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin_page</title>
    <style>
        body{
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            display: flex;
            height: 100vh;
        }
        .sidebar{
            width: 280px;
            background: whitesmoke;
            padding: 20px;
            border-right: 1px solid #ccc;
        }
        .sidebar input, .sidebar select, .sidebar button{
            width: 100%;
            margin-bottom: 12px;
            padding: 8px;
           
        }

        .main{
            width: 100%;
            padding: 20px;
            overflow: auto;
        }
        .navbar select, .navbar button{
            padding: 8px 12px;
        }
        .fleet-container{
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            padding-right: 20px;
        }
        .card{
            background: white;
            padding:20px;
            border-radius: 8px;
            border: 1px solid #ddd;
            box-shadow: 0 4px 10px rgba(red, green, blue, alpha);
            width: 95%;
            margin: auto;
        }
        .card img{
            width: 100%;
            height:160px;
            object-fit: contain;
            border-radius: 4px;
        }
        .card h3, 
        .card p{
            margin:8px 0;
            font-size: 15px;
        }

        .card h3{
            font-size: 16px;
            font-weight: 600;
        }
        .card button{
            width: 100px;
            padding: 8px;
            margin-top: 8px;
            background: white;
            border: 1px solid #888;
            border-radius: 4px;
            cursor: pointer;
        }
        .card button :hover{
            background: white;
        }
</head>
<body>
    <div class="sidebar">
        <h2> Add Fleet </h2>
        <input type="text" id="regNo" ::placeholder="Reg No of vehicle">
        <select id="category">
            <option value="">Select Category</option>
            <option> Auto</option>
            <option> Car</option>
            <option> Truck</option>
            <option> Bus</option>
        </select>
        <input type="text" id="driverName" ::placeholder="<Driver Name"> 
            <select id="unavailable">
             <option value="Available">Available</option>    
             <option value="Unavailable">Unavailable</option>
            </select>
        <button onclick="addFleet()">Add Fleet</button>
    </div>
    <div class="main">
        <div class="navbar">
            <select id="filtercategory" onchange="applyFilters()">
                <option value="">All catories</option> 
                 <option> Auto</option>
                 <option> Car</option>
                 <option> Truck</option>
                <option> Bus</option>
            </select>

            <select id="filterAvailability" onchange="applyFilters()">
                <option value="">All Availability</option>
                <option>Available</option>    
             <option>Unavailable</option>
            </select>

            <button onclick="clearFilters()">Clear Filter</button>
        </div>
        <div class="fleet-container" id="fleet-container"></div>
        </div>

        <script>
            let fleetData =[];
             
           const vachicleImage = "https://coding-platform.s3.amazonaws.com/dev/lms/tickets/5e80fcb6-3f8e-480c-945b-30a5359eb40e/JNmYjkVr3WOjsrbu.png";
           function addFleet(){
            let reg=document.getElementById("regNo").value.trim();
            let cat=document.getElementById("category").value;
            let driver=document.getElementById("drivername").value.trim();
            let available=document.getElementById("isAvailable").value;

            if(reg===""||driver===""||cat===""){
                alert("All feilds are required.");
                return;
            }
            let obj={
                reg,
                cat,
                driver,
                available

            };
            fleetData.push(obj);
            renderCards();
            clearForm();
           }
           function clearForm(){
            document.getElementById("region").value="";
            document.getElementById("category").value="";
            document.getElementById("drivername").value="";
            document.getElementById("isAvailable").value="";
           }

           function renderCards(filtered=null){
            let container=document.getElementById("fleetContainer");
            container.innerHTML="";
           }

           funtion updateDriver(i){
            let newName=prompt("Enter new driver name:");
            if(newname===null)return;
            if(newname.trim()===""){
                alert("driver name cannot be empty.");
                return;
            }

            function changeAvailability(i){
                renderCards();
            }
            function deleteVehicles(i){
                renderCards();
            }
            function ApplyFilters(){
                 renderCards(Filtered);
            }
            fuction clearFilters(){
                document.getElementById("filterCategory").value="";
                document.getElementById("filterAvailability").value="";
                returnCards();
            }
            renderCards();
           }

</script>

    
</body>
</html>
